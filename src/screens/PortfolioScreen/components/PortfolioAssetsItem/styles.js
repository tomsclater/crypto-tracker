import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    color: "white",
    fontSize: 16,
    fontWeight: "bold",
  },
  ticker: {
    color: "grey",
    fontWeight: "600",
  },
  coinContainer: {
    flexDirection: "row",
  },
});

export default styles;
